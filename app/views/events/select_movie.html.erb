



<%= form_with(model: @event, local: true) do |event_form| %>

   <div class="m-12">

        <div class="overflow-x-auto">
            
            <div class="min-w-screen bg-gray-100 flex items-center justify-center overscroll-auto font-sans overflow-hidden">
                

                <table class="min-w-max w-full table-auto">

                    <thead>
                        <tr class="bg-green-200 text-green-600 text-sm">

                        <th colspan="3"></th>
                        </tr>
                    </thead>

                    <tbody>



                            <% for item in @movies %>

                                <tr>
                                    <td class="m-9"><%= item["title"] %></td>
                                    <td class="m-9"><%= link_to 'Add movie', add_movie(item["title"]) %></td>
                                </tr>



                            <% end %>

                    </tbody>
                </table>
            </div>
        </div>
    </div>

<% end %>